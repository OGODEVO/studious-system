# OpenAI API Key (Required if not set in config.yaml provider config)
OPENAI_API_KEY=sk-...

# Novita API Key (if using provider: novita)
NOVITA_API_KEY=...

# Perplexity Search API Key (for real-time search tool)
PERPLEXITY_API_KEY=...

# Optional Overrides (config.yaml defaults usually suffice)
# OASIS_MODEL=gpt-4o
# OASIS_HEADLESS=true
